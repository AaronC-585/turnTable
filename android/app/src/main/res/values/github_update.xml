<?xml version="1.0" encoding="utf-8"?>
<!--
  Update checks (raw manifest JSON, no GitHub API):
  1) CurrentVersion.json at repo root — version, releasePageUrl, assets.apk (URLs). Gradle writes it each :app build;
     scripts/release-github.sh refreshes it after a release with exact asset URLs.
  2) Legacy: CurrentVersion.txt (one line) then android/app/update-check-latest-version.txt.

  Set github_update_owner / github_update_repo (used by Gradle for default URLs in the manifest). Leave owner empty to disable checks.
  github_update_version_ref = branch or tag for the raw URL (usually main).
-->
<resources>
    <string name="github_update_owner" translatable="false">AaronC-585</string>
    <string name="github_update_repo" translatable="false">turnTable</string>
    <string name="github_update_version_ref" translatable="false">main</string>
</resources>
